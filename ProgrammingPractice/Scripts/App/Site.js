/*===============================================
                    Site
===============================================*/

/*=============================
           Variables
=============================*/

/*=============================
           Methods
=============================*/
var exists = function (value) {
    return !(value == null || value == '' || value.length == null || value.length <= 0);
}